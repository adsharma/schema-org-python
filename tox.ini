[tox]
envlist = py3{10,12}
skip_missing_interpreters = true

[testenv]
passenv =
    HOME
    LocalAppData
deps =
    pytest-cov
    pypika
    duckdb-engine
    inflection
    polars
commands =
    pytest --cov -rs -v {posargs}
